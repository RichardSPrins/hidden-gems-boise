---
// src/pages/explore/[category]/[subcategory].astro
import PublicLayout from "@/layouts/PublicLayout.astro";
import { GET } from "@/pages/api/web/explore/category";

const categoryHeroImages: Record<string, string> = {
  "food-and-drink":
    "https://images.unsplash.com/photo-1414235077428-338989a2e8c0?w=1600&auto=format&fit=crop&q=80",
  "health-and-wellness":
    "https://images.unsplash.com/photo-1544367567-0f2fcb009e0b?w=1600&auto=format&fit=crop&q=80",
  shopping:
    "https://images.unsplash.com/photo-1441984904996-e0b6ba687e04?w=1600&auto=format&fit=crop&q=80",
  "home-services":
    "https://images.unsplash.com/photo-1558618666-fcd25c85cd64?w=1600&auto=format&fit=crop&q=80",
  "arts-and-entertainment":
    "https://images.unsplash.com/photo-1514525253161-7a46d19cd819?w=1600&auto=format&fit=crop&q=80",
  "professional-services":
    "https://images.unsplash.com/photo-1497366216548-37526070297c?w=1600&auto=format&fit=crop&q=80",
  education:
    "https://images.unsplash.com/photo-1503676260728-1c00da094a0b?w=1600&auto=format&fit=crop&q=80",
  automotive:
    "https://images.unsplash.com/photo-1487754180451-c456f719a1fc?w=1600&auto=format&fit=crop&q=80",
};

const priceMap: Record<string, string> = {
  ONE: "$",
  TWO: "$$",
  THREE: "$$$",
  FOUR: "$$$$",
};

const priceOptions = [
  { value: "ONE", label: "$" },
  { value: "TWO", label: "$$" },
  { value: "THREE", label: "$$$" },
  { value: "FOUR", label: "$$$$" },
];

// ─── URL Params ───────────────────────────────────────────
const { category: categoryParam, subcategory: subcategoryParam } = Astro.params;
const url = new URL(Astro.request.url);
const q = url.searchParams.get("q") ?? "";
const neighborhood = url.searchParams.get("neighborhood") ?? "";
const pricePoints = url.searchParams.getAll("price");
const sort = url.searchParams.get("sort") ?? "rating";
const page = Math.max(1, parseInt(url.searchParams.get("page") ?? "1"));

// ─── API Call ─────────────────────────────────────────────
const apiUrl = new URL(Astro.request.url);
apiUrl.searchParams.set("categorySlug", categoryParam ?? "");
apiUrl.searchParams.set("subcategory", subcategoryParam ?? "");

const response = await GET({
  request: new Request(apiUrl.toString()),
  params: Astro.params,
} as any);
const data = await response.json();

if (data.notFound) {
  return Astro.redirect("/explore");
}

const {
  results,
  allCategories,
  activeCategory,
  neighborhoods,
  count,
  totalPages,
} = data;

const activeSubcategory = activeCategory.subcategories.find(
  (s: any) => s.slug === subcategoryParam
);

if (!activeSubcategory) {
  return Astro.redirect(`/explore/${activeCategory.slug}`);
}

const heroImage =
  categoryHeroImages[activeCategory.slug] ??
  "https://images.unsplash.com/photo-1477959858617-67f85cf4f1df?w=1600&auto=format&fit=crop&q=80";
const heroDescription = `Discover the best ${activeSubcategory.name} across Boise and the Treasure Valley.`;

// ─── Helpers ──────────────────────────────────────────────
function renderStars(rating: number) {
  const full = Math.floor(rating);
  const empty = 5 - full;
  return "★".repeat(full) + "☆".repeat(empty);
}

function buildUrl(overrides: Record<string, string | string[] | null>) {
  const params = new URLSearchParams();
  if (q) params.set("q", q);
  if (neighborhood) params.set("neighborhood", neighborhood);
  pricePoints.forEach((p: string) => params.append("price", p));
  params.set("sort", sort);
  params.set("page", "1");
  Object.entries(overrides).forEach(([k, v]) => {
    params.delete(k);
    if (Array.isArray(v)) v.forEach((val) => params.append(k, val));
    else if (v !== null) params.set(k, v);
  });
  const base = `/explore/${activeCategory.slug}/${activeSubcategory.slug}`;
  const qs = params.toString();
  return qs ? `${base}?${qs}` : base;
}

const formAction = `/explore/${activeCategory.slug}/${activeSubcategory.slug}`;
const hasFilters = q || neighborhood || pricePoints.length > 0;
---

<PublicLayout
  title={`Best ${activeSubcategory.name} in Boise, ID`}
  description={heroDescription}
>
  <!-- SLIM HERO -->
  <div class="explore-hero">
    <div
      class="explore-hero-bg"
      style={`background-image: url('${heroImage}')`}
    >
    </div>
    <div class="explore-hero-overlay"></div>
    <div class="explore-hero-content section-container">
      <nav class="hero-breadcrumb" aria-label="Breadcrumb">
        <a href="/explore">Explore</a>
        <span>›</span>
        <a href={`/explore/${activeCategory.slug}`}>{activeCategory.name}</a>
        <span>›</span>
        <span>{activeSubcategory.name}</span>
      </nav>
      <h1 class="explore-hero-title">
        Best <em>{activeSubcategory.name}</em> in Boise, ID
      </h1>
      <p class="explore-hero-sub">{heroDescription}</p>
    </div>
  </div>

  <!-- MAIN LAYOUT -->
  <div class="explore-layout section-container">
    <!-- SIDEBAR -->
    <aside class="explore-sidebar">
      <div class="sidebar-inner">
        <form method="get" action={formAction} class="sidebar-section">
          <label class="sidebar-label">Search</label>
          <div class="sidebar-search-row">
            <input
              type="text"
              name="q"
              value={q}
              placeholder="Keywords…"
              class="gem-input sidebar-search-input"
            />
            <input type="hidden" name="sort" value={sort} />
            <button type="submit" class="sidebar-search-btn">Go</button>
          </div>
        </form>

        <div class="sidebar-divider"></div>

        <div class="sidebar-section">
          <p class="sidebar-label">Type</p>
          <ul class="sidebar-cat-list">
            <li>
              <a
                href={`/explore/${activeCategory.slug}`}
                class="sidebar-cat-item"
              >
                All {activeCategory.name}
              </a>
            </li>
            {activeCategory.subcategories.map((sub: any) => (
              <li>
                <a
                  href={`/explore/${activeCategory.slug}/${sub.slug}`}
                  class:list={[
                    "sidebar-cat-item",
                    sub.slug === activeSubcategory.slug &&
                      "sidebar-cat-active",
                  ]}
                >
                  {sub.name}
                </a>
              </li>
            ))}
          </ul>
        </div>

        <div class="sidebar-divider"></div>

        <div class="sidebar-section">
          <p class="sidebar-label">Browse Categories</p>
          <ul class="sidebar-cat-list">
            {
              allCategories.map((cat: any) => (
                <li>
                  <a
                    href={`/explore/${cat.slug}`}
                    class:list={[
                      "sidebar-cat-item",
                      cat.slug === activeCategory.slug && "sidebar-cat-active",
                    ]}
                  >
                    {cat.name}
                  </a>
                </li>
              ))
            }
          </ul>
        </div>

        <div class="sidebar-divider"></div>

        <form method="get" action={formAction} class="sidebar-section">
          <p class="sidebar-label">Price Range</p>
          <input type="hidden" name="q" value={q} />
          <input type="hidden" name="sort" value={sort} />
          <div class="price-pills">
            {
              priceOptions.map((opt) => (
                <label
                  class:list={[
                    "price-pill",
                    pricePoints.includes(opt.value) && "price-pill-active",
                  ]}
                >
                  <input
                    type="checkbox"
                    name="price"
                    value={opt.value}
                    checked={pricePoints.includes(opt.value)}
                    class="sr-only"
                    onchange="this.form.submit()"
                  />
                  {opt.label}
                </label>
              ))
            }
          </div>
        </form>

        {
          neighborhoods.length > 0 && (
            <>
              <div class="sidebar-divider" />
              <div class="sidebar-section">
                <p class="sidebar-label">Neighborhood</p>
                <ul class="sidebar-neighborhood-list">
                  <li>
                    <a
                      href={buildUrl({ neighborhood: null })}
                      class:list={[
                        "sidebar-neighborhood-item",
                        !neighborhood && "sidebar-neighborhood-active",
                      ]}
                    >
                      All Neighborhoods
                    </a>
                  </li>
                  {neighborhoods.map((n: string) => (
                    <li>
                      <a
                        href={buildUrl({ neighborhood: n })}
                        class:list={[
                          "sidebar-neighborhood-item",
                          neighborhood === n && "sidebar-neighborhood-active",
                        ]}
                      >
                        {n}
                      </a>
                    </li>
                  ))}
                </ul>
              </div>
            </>
          )
        }

        {
          hasFilters && (
            <>
              <div class="sidebar-divider" />
              <div class="sidebar-section">
                <a href={formAction} class="sidebar-clear">
                  ✕ Clear filters
                </a>
              </div>
            </>
          )
        }
      </div>
    </aside>

    <!-- RESULTS -->
    <div class="explore-results">
      <div class="results-header">
        <p class="results-count">
          {Number(count).toLocaleString()}
          {Number(count) === 1 ? "result" : "results"}
          {
            q && (
              <span>
                {" "}
                for "<strong>{q}</strong>"
              </span>
            )
          }
        </p>
        <div class="results-controls">
          <button
            class="mobile-filter-btn"
            id="mobile-filter-btn"
            aria-label="Open filters"
          >
            <span aria-hidden="true">⚙</span>
            <span>Filters</span>
            {hasFilters && <span class="filter-dot" />}
          </button>
          <form method="get" action={formAction} class="sort-form">
            <input type="hidden" name="q" value={q} />
            <input type="hidden" name="neighborhood" value={neighborhood} />
            {
              pricePoints.map((p: string) => (
                <input type="hidden" name="price" value={p} />
              ))
            }
            <select
              name="sort"
              class="sort-select"
              aria-label="Sort"
              onchange="this.form.submit()"
            >
              <option value="rating" selected={sort === "rating"}>Top rated</option>
              <option value="newest" selected={sort === "newest"}>Newest</option>
              <option value="az" selected={sort === "az"}>A–Z</option>
            </select>
          </form>
        </div>
      </div>

      {
        hasFilters && (
          <div class="filter-chips">
            {q && (
              <a href={buildUrl({ q: null })} class="filter-chip">
                "{q}" ✕
              </a>
            )}
            {neighborhood && (
              <a href={buildUrl({ neighborhood: null })} class="filter-chip">
                {neighborhood} ✕
              </a>
            )}
            {pricePoints.map((p: string) => (
              <a
                href={buildUrl({
                  price: pricePoints.filter((x: string) => x !== p),
                })}
                class="filter-chip"
              >
                {priceMap[p]} ✕
              </a>
            ))}
          </div>
        )
      }

      {
        results.length > 0 ? (
          <ul class="biz-list">
            {results.map((biz: any) => (
              <li>
                <a href={`/listings/${biz.slug}`} class="biz-list-card">
                  <div class="biz-list-img">
                    {biz.images[0] ? (
                      <img
                        src={biz.images[0].url}
                        alt={biz.images[0].alt ?? biz.name}
                        loading="lazy"
                      />
                    ) : (
                      <span class="biz-list-img-placeholder" aria-hidden="true" />
                    )}
                    {/* {biz.isGemOfWeek && (
                      <span class="biz-list-gem-badge">💎 Gem</span>
                    )} */}
                  </div>
                  <div class="biz-list-body">
                    <div class="biz-list-row-1">
                      <span class="biz-card-cat">{biz.category.name}</span>
                      {biz.subcategory && (
                        <span class="biz-list-subcat">
                          · {biz.subcategory.name}
                        </span>
                      )}
                    </div>
                    <div class="biz-list-row-2">
                      <h2 class="biz-list-name">{biz.name}</h2>
                      {biz.reviewCount > 0 && (
                        <div class="biz-list-rating">
                          <span class="biz-list-stars">
                            {renderStars(biz.averageRating)}
                          </span>
                          <span class="biz-list-rating-num">
                            {biz.averageRating.toFixed(1)}
                          </span>
                          <span class="biz-list-rating-count">
                            ({biz.reviewCount})
                          </span>
                        </div>
                      )}
                    </div>
                    <div class="biz-list-row-3">
                      {biz.pricePoint && (
                        <span class="biz-list-price">
                          {priceMap[biz.pricePoint]}
                        </span>
                      )}
                      {biz.locations[0]?.neighborhood && (
                        <span class="biz-list-location">
                          {biz.locations[0].neighborhood}, Boise
                        </span>
                      )}
                    </div>
                    {biz.bio && (
                      <p class="biz-list-bio">
                        {biz.bio.length > 160
                          ? biz.bio.slice(0, 157) + "…"
                          : biz.bio}
                      </p>
                    )}
                    <div class="biz-list-footer">
                      <div class="biz-list-tags">
                        {biz.isVerified && (
                          <span class="verified-badge">✓ Verified</span>
                        )}
                      </div>
                      <span class="biz-list-cta">View →</span>
                    </div>
                  </div>
                </a>
              </li>
            ))}
          </ul>
        ) : (
          <div class="no-results">
            <p class="no-results-icon">🔍</p>
            <h3 class="no-results-title">No results found</h3>
            <p class="no-results-sub">
              Try adjusting your filters or search terms.
            </p>
            <a href={formAction} class="btn-gold">
              Clear Filters
            </a>
          </div>
        )
      }

      {
        totalPages > 1 && (
          <div class="explore-pagination">
            {page > 1 && (
              <a href={buildUrl({ page: String(page - 1) })} class="page-btn">
                ← Previous
              </a>
            )}
            <div class="page-numbers">
              {Array.from(
                { length: totalPages },
                (_: any, i: number) => i + 1
              ).map((p: number) => (
                <a
                  href={buildUrl({ page: String(p) })}
                  class:list={["page-num", p === page && "page-num-active"]}
                >
                  {p}
                </a>
              ))}
            </div>
            {page < totalPages && (
              <a href={buildUrl({ page: String(page + 1) })} class="page-btn">
                Next →
              </a>
            )}
          </div>
        )
      }
    </div>
  </div>

  <!-- Mobile drawer -->
  <div class="mobile-drawer-overlay hidden" id="drawer-overlay"></div>
  <div class="mobile-drawer hidden" id="mobile-drawer" aria-hidden="true">
    <div class="mobile-drawer-header">
      <h2 class="mobile-drawer-title">Filters</h2>
      <button class="mobile-drawer-close" id="drawer-close" aria-label="Close"
        >✕</button
      >
    </div>
    <div class="mobile-drawer-body">
      <form method="get" action={formAction} class="sidebar-section">
        <label class="sidebar-label">Search</label>
        <div class="sidebar-search-row">
          <input
            type="text"
            name="q"
            value={q}
            placeholder="Keywords…"
            class="gem-input"
          />
          <input type="hidden" name="sort" value={sort} />
          <button type="submit" class="sidebar-search-btn">Go</button>
        </div>
      </form>
      <div class="sidebar-divider"></div>
      <div class="sidebar-section">
        <p class="sidebar-label">Type</p>
        <ul class="sidebar-cat-list">
          <li>
            <a
              href={`/explore/${activeCategory.slug}`}
              class="sidebar-cat-item"
            >
              All {activeCategory.name}
            </a>
          </li>
          {activeCategory.subcategories.map((sub: any) => (
            <li>
              <a
                href={`/explore/${activeCategory.slug}/${sub.slug}`}
                class:list={[
                  "sidebar-cat-item",
                  sub.slug === activeSubcategory.slug && "sidebar-cat-active",
                ]}
              >
                {sub.name}
              </a>
            </li>
          ))}
        </ul>
      </div>
      <div class="sidebar-divider"></div>
      <form method="get" action={formAction} class="sidebar-section">
        <p class="sidebar-label">Price Range</p>
        <input type="hidden" name="q" value={q} />
        <input type="hidden" name="sort" value={sort} />
        <div class="price-pills">
          {
            priceOptions.map((opt) => (
              <label
                class:list={[
                  "price-pill",
                  pricePoints.includes(opt.value) && "price-pill-active",
                ]}
              >
                <input
                  type="checkbox"
                  name="price"
                  value={opt.value}
                  checked={pricePoints.includes(opt.value)}
                  class="sr-only"
                  onchange="this.form.submit()"
                />
                {opt.label}
              </label>
            ))
          }
        </div>
      </form>
      {
        hasFilters && (
          <div class="sidebar-section">
            <a href={formAction} class="sidebar-clear">
              ✕ Clear filters
            </a>
          </div>
        )
      }
    </div>
  </div>
</PublicLayout>

<style>
  .explore-hero {
    position: relative;
    min-height: 300px;
    display: flex;
    align-items: flex-end;
    overflow: hidden;
    padding-top: 5.5rem;
  }

  .explore-hero-bg {
    position: absolute;
    inset: 0;
    background-size: cover;
    background-position: center 40%;
  }

  .explore-hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to bottom,
      rgba(13, 13, 13, 0.35) 0%,
      rgba(13, 13, 13, 0.78) 100%
    );
  }

  .explore-hero-content {
    position: relative;
    z-index: 2;
    padding-bottom: 2.5rem;
    width: 100%;
  }

  .hero-breadcrumb {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.5);
    margin-bottom: 0.75rem;
  }

  .hero-breadcrumb a {
    color: rgba(255, 255, 255, 0.5);
    text-decoration: none;
    transition: color 0.15s;
  }
  .hero-breadcrumb a:hover {
    color: var(--color-gem-gold);
  }

  .explore-hero-title {
    font-family: var(--font-display);
    font-size: clamp(2.25rem, 5vw, 4rem);
    font-weight: 600;
    color: white;
    line-height: 1.1;
    margin-bottom: 0.5rem;
  }

  .explore-hero-title em {
    font-style: italic;
    color: var(--color-gem-gold-light);
  }
  .explore-hero-sub {
    color: rgba(255, 255, 255, 0.65);
    font-size: 0.9375rem;
    max-width: 560px;
  }

  .explore-layout {
    display: grid;
    grid-template-columns: 260px 1fr;
    gap: 2rem;
    padding-top: 2rem;
    padding-bottom: 4rem;
    align-items: start;
  }

  @media (max-width: 1024px) {
    .explore-layout {
      grid-template-columns: 1fr;
    }
    .explore-sidebar {
      display: none;
    }
  }

  .explore-sidebar {
    position: sticky;
    top: 120px;
  }

  .sidebar-inner {
    background: white;
    border-radius: 12px;
    border: 1.5px solid var(--color-gem-border);
    overflow: hidden;
  }
  .sidebar-section {
    padding: 1.25rem;
  }
  .sidebar-label {
    display: block;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--color-gem-text-subtle);
    margin-bottom: 0.75rem;
  }
  .sidebar-divider {
    height: 1px;
    background: var(--color-gem-border);
  }
  .sidebar-search-row {
    display: flex;
    gap: 0.5rem;
  }
  .sidebar-search-input {
    flex: 1;
    font-size: 0.875rem;
    padding: 0.625rem 0.875rem;
  }
  .sidebar-search-btn {
    padding: 0.625rem 0.875rem;
    background: var(--color-gem-gold);
    color: var(--color-gem-dark);
    border: none;
    border-radius: 8px;
    font-size: 0.8125rem;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.2s;
  }
  .sidebar-search-btn:hover {
    background: var(--color-gem-gold-light);
  }
  .sidebar-cat-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.125rem;
  }
  .sidebar-cat-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 0.75rem;
    border-radius: 8px;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--color-gem-text-muted);
    text-decoration: none;
    transition: all 0.15s;
  }
  .sidebar-cat-item:hover {
    background: var(--color-gem-mist);
    color: var(--color-gem-text);
  }
  .sidebar-cat-active {
    background: rgba(201, 168, 76, 0.08);
    color: var(--color-gem-gold);
    font-weight: 600;
  }
  .price-pills {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }
  .price-pill {
    padding: 0.375rem 0.875rem;
    border-radius: 100px;
    border: 1.5px solid var(--color-gem-border);
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-gem-text-muted);
    cursor: pointer;
    transition: all 0.15s;
  }
  .price-pill:hover {
    border-color: var(--color-gem-gold);
    color: var(--color-gem-gold);
  }
  .price-pill-active {
    background: var(--color-gem-gold);
    border-color: var(--color-gem-gold);
    color: var(--color-gem-dark);
  }
  .sidebar-neighborhood-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.125rem;
    max-height: 200px;
    overflow-y: auto;
  }
  .sidebar-neighborhood-item {
    display: block;
    padding: 0.4rem 0.75rem;
    border-radius: 6px;
    font-size: 0.8125rem;
    color: var(--color-gem-text-muted);
    text-decoration: none;
    transition: all 0.15s;
  }
  .sidebar-neighborhood-item:hover {
    background: var(--color-gem-mist);
    color: var(--color-gem-text);
  }
  .sidebar-neighborhood-active {
    color: var(--color-gem-gold);
    font-weight: 600;
  }
  .sidebar-clear {
    display: block;
    font-size: 0.8125rem;
    color: var(--color-gem-text-subtle);
    text-decoration: none;
    transition: color 0.15s;
  }
  .sidebar-clear:hover {
    color: var(--color-gem-text);
  }

  .results-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 1rem;
    flex-wrap: wrap;
  }
  .results-count {
    font-size: 0.875rem;
    color: var(--color-gem-text-muted);
    margin: 0;
  }
  .results-count strong { color: var(--color-gem-text); font-weight: 600; }
  .results-controls {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-shrink: 0;
  }
  .mobile-filter-btn {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    padding: 0.5rem 0.875rem;
    border: 1.5px solid var(--color-gem-border);
    border-radius: 8px;
    background: white;
    font-family: var(--font-body);
    font-size: 0.8125rem;
    font-weight: 500;
    color: var(--color-gem-text);
    cursor: pointer;
    transition: border-color 0.15s;
  }
  .mobile-filter-btn:hover { border-color: var(--color-gem-gold); }
  .filter-dot {
    width: 8px;
    height: 8px;
    background: var(--color-gem-gold);
    border-radius: 50%;
    margin-left: 0.125rem;
  }
  @media (min-width: 1025px) {
    .mobile-filter-btn { display: none; }
  }
  .sort-select {
    padding: 0.5rem 0.875rem;
    border: 1.5px solid var(--color-gem-border);
    border-radius: 8px;
    font-family: var(--font-body);
    font-size: 0.8125rem;
    color: var(--color-gem-text);
    background: white;
    outline: none;
    cursor: pointer;
    appearance: none;
    background-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='10' height='10' viewBox='0 0 12 12'><path fill='%236b6b6b' d='M2 4l4 4 4-4z'/></svg>");
    background-repeat: no-repeat;
    background-position: right 0.6rem center;
    padding-right: 1.75rem;
  }
  .sort-select:focus { border-color: var(--color-gem-gold); }

  .filter-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 0.375rem;
    margin-bottom: 1rem;
  }
  .filter-chip {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    padding: 0.25rem 0.625rem;
    background: var(--color-gem-mist-2);
    border: none;
    border-radius: 100px;
    font-size: 0.75rem;
    color: var(--color-gem-text-muted);
    text-decoration: none;
    font-weight: 500;
    transition: background 0.15s, color 0.15s;
  }
  .filter-chip:hover {
    background: var(--color-gem-border);
    color: var(--color-gem-text);
  }

  .biz-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .biz-list-card {
    display: grid;
    grid-template-columns: 160px 1fr;
    background: white;
    border-radius: 12px;
    border: 1.5px solid var(--color-gem-border);
    overflow: hidden;
    text-decoration: none;
    color: inherit;
    transition: all 0.2s;
  }
  .biz-list-card:hover {
    border-color: var(--color-gem-gold);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
    transform: translateY(-2px);
  }
  @media (max-width: 768px) {
    .biz-list-card {
      grid-template-columns: 1fr;
    }
    .biz-list-img {
      aspect-ratio: 16/9;
      width: 100%;
    }
  }
  .biz-list-img {
    position: relative;
    background: var(--color-gem-mist-2);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }
  .biz-list-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .biz-list-img-placeholder {
    font-size: 2.5rem;
  }
  .biz-list-gem-badge {
    position: absolute;
    top: 0.5rem;
    left: 0.5rem;
    background: var(--color-gem-gold);
    color: var(--color-gem-dark);
    font-size: 0.65rem;
    font-weight: 700;
    padding: 0.2rem 0.5rem;
    border-radius: 100px;
    letter-spacing: 0.05em;
  }
  .biz-list-body {
    padding: 1.25rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .biz-list-row-1 {
    display: flex;
    align-items: center;
    gap: 0.375rem;
    flex-wrap: wrap;
  }
  .biz-list-subcat {
    font-size: 0.7rem;
    color: var(--color-gem-text-subtle);
  }
  .biz-list-row-2 {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 0.75rem;
  }
  @media (max-width: 480px) {
    .biz-list-row-2 {
      flex-direction: column;
      gap: 0.25rem;
    }
  }
  .biz-list-name {
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-gem-text);
    line-height: 1.2;
    margin: 0;
  }
  .biz-list-rating {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    flex-shrink: 0;
    white-space: nowrap;
  }
  .biz-list-stars {
    color: var(--color-gem-gold);
    font-size: 0.8rem;
  }
  .biz-list-rating-num {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-gem-text);
  }
  .biz-list-rating-count {
    font-size: 0.8rem;
    color: var(--color-gem-text-subtle);
  }
  .biz-list-row-3 {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }
  .biz-list-price {
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--color-gem-text-muted);
  }
  .biz-list-location {
    font-size: 0.8rem;
    color: var(--color-gem-text-subtle);
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }
  .biz-list-bio {
    font-size: 0.875rem;
    color: var(--color-gem-text-muted);
    line-height: 1.6;
    margin: 0;
  }
  .biz-list-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-top: auto;
    padding-top: 0.375rem;
  }
  .biz-list-tags {
    display: flex;
    gap: 0.375rem;
    flex-wrap: wrap;
  }
  .biz-list-cta {
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--color-gem-gold);
    white-space: nowrap;
  }

  .no-results {
    text-align: center;
    padding: 4rem 2rem;
    background: white;
    border-radius: 12px;
    border: 1.5px solid var(--color-gem-border);
  }
  .no-results-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }
  .no-results-title {
    font-family: var(--font-display);
    font-size: 1.75rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
  }
  .no-results-sub {
    color: var(--color-gem-text-muted);
    margin-bottom: 1.5rem;
  }

  .explore-pagination {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    margin-top: 2rem;
    padding-top: 2rem;
    border-top: 1px solid var(--color-gem-border);
  }
  .page-btn {
    padding: 0.5rem 1rem;
    border: 1.5px solid var(--color-gem-border);
    border-radius: 8px;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--color-gem-text-muted);
    text-decoration: none;
    transition: all 0.15s;
  }
  .page-btn:hover {
    border-color: var(--color-gem-gold);
    color: var(--color-gem-gold);
  }
  .page-numbers {
    display: flex;
    gap: 0.25rem;
  }
  .page-num {
    width: 2.25rem;
    height: 2.25rem;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 8px;
    font-size: 0.875rem;
    font-weight: 500;
    text-decoration: none;
    color: var(--color-gem-text-muted);
    border: 1.5px solid transparent;
    transition: all 0.15s;
  }
  .page-num:hover {
    border-color: var(--color-gem-border);
    color: var(--color-gem-text);
  }
  .page-num-active {
    background: var(--color-gem-gold);
    color: var(--color-gem-dark);
    font-weight: 700;
  }

  .mobile-drawer-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.5);
    z-index: 80;
  }
  .mobile-drawer {
    position: fixed;
    top: 0;
    left: 0;
    bottom: 0;
    width: min(320px, 85vw);
    background: white;
    z-index: 90;
    overflow-y: auto;
    transform: translateX(-100%);
    transition: transform 0.3s ease;
  }
  .mobile-drawer.drawer-open {
    transform: translateX(0);
  }
  .mobile-drawer-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.25rem;
    border-bottom: 1px solid var(--color-gem-border);
    position: sticky;
    top: 0;
    background: white;
    z-index: 1;
  }
  .mobile-drawer-title {
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 600;
    margin: 0;
  }
  .mobile-drawer-close {
    background: none;
    border: none;
    font-size: 1.25rem;
    cursor: pointer;
    color: var(--color-gem-text-muted);
    padding: 0.25rem;
    line-height: 1;
  }
</style>

<script>
  const filterBtn = document.getElementById("mobile-filter-btn");
  const drawer = document.getElementById("mobile-drawer");
  const overlay = document.getElementById("drawer-overlay");
  const closeBtn = document.getElementById("drawer-close");

  function openDrawer() {
    drawer?.classList.remove("hidden");
    overlay?.classList.remove("hidden");
    setTimeout(() => drawer?.classList.add("drawer-open"), 10);
    document.body.style.overflow = "hidden";
  }

  function closeDrawer() {
    drawer?.classList.remove("drawer-open");
    document.body.style.overflow = "";
    setTimeout(() => {
      drawer?.classList.add("hidden");
      overlay?.classList.add("hidden");
    }, 300);
  }

  filterBtn?.addEventListener("click", openDrawer);
  closeBtn?.addEventListener("click", closeDrawer);
  overlay?.addEventListener("click", closeDrawer);
</script>
